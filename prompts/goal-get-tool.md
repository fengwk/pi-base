Read the current persisted goal, including its objective, status, usage, and remaining token budget. Use the returned goal to advance the objective or check whether every objective requirement is already satisfied. For an active goal, when current evidence proves it is genuinely complete, call `update_goal` with status `complete` and a detailed reason with the supporting evidence; when it is blocked under the goal policy, call `update_goal` with status `blocked` and a detailed reason describing the blocker and evidence. For a `budget_limited` goal, call `update_goal` only when current evidence proves it is genuinely complete; otherwise leave it `budget_limited`.
