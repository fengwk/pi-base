Read the current persisted goal, including its objective, status, usage, and remaining token budget. Use the returned goal to advance the objective or check whether every objective requirement is already satisfied. When current evidence proves the goal is complete, call `update_goal` with status `complete` and a detailed reason with the supporting evidence; when it is blocked under the goal policy, call `update_goal` with status `blocked` and a detailed reason describing the blocker and evidence.
